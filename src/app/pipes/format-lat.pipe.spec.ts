import { FormatLatPipe } from './format-lat.pipe';

describe('FormatLatPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLatPipe();
    expect(pipe).toBeTruthy();
  });
});
