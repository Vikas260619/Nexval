import { FormatLongPipe } from './format-long.pipe';

describe('FormatLongPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLongPipe();
    expect(pipe).toBeTruthy();
  });
});
