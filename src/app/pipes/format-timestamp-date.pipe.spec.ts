import { FormatTimestampDatePipe } from './format-timestamp-date.pipe';

describe('FormatTimestampDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTimestampDatePipe();
    expect(pipe).toBeTruthy();
  });
});
