import { FormatTimestampTimePipe } from './format-timestamp-time.pipe';

describe('FormatTimestampTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTimestampTimePipe();
    expect(pipe).toBeTruthy();
  });
});
