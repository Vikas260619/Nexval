import { FormatonlyDatePipe } from './formatonly-date.pipe';

describe('FormatonlyDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatonlyDatePipe();
    expect(pipe).toBeTruthy();
  });
});
